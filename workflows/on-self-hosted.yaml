# Managed by the wfnexus workflow builder.
# Hand edits are fine; the builder round-trips through the same loader.
name: on-self-hosted
description: ""
input_schema:
    type: object
runs-on: self-hosted
steps: []
jobs:
    check:
        steps:
            - id: where
              name: ""
              prompt: ""
              skills: []
              tools: []
              output_schema:
                properties:
                    exitCode:
                        type: integer
                    ok:
                        type: boolean
                    stderr:
                        type: string
                    stdout:
                        type: string
                type: object
              run: echo "WFX_RUNNER='${WFX_RUNNER:-<unset>}'"
max_parallel: 4
