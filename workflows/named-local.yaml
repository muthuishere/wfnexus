# Managed by the wfnexus workflow builder.
# Hand edits are fine; the builder round-trips through the same loader.
name: named-local
description: runs-on names a label the platform serves itself.
input_schema:
    type: object
runs-on: local
steps: []
jobs:
    check:
        steps:
            - id: where
              name: ""
              prompt: ""
              skills: []
              tools: []
              output_schema:
                properties:
                    exitCode:
                        type: integer
                    ok:
                        type: boolean
                    stderr:
                        type: string
                    stdout:
                        type: string
                type: object
              run: echo "ran in-process; no worker needed"
max_parallel: 4
